<section class="card">
    <a href="/post/{{id}}">
      <header>{{post_title}}</header>
    </a>
    <p>{{date_created}}</p>
    <p>{{user.name}}</p>
    <div class="card-body">
      <p>{{comment}}</p>
    </div>
</section>